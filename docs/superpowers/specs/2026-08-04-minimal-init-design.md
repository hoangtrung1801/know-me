# Minimal Init Design

`knowns init` becomes a lightweight project registration and configuration flow.
The wizard asks only for the project name. Initialization defaults to tracking
all Know-Me sections in Git and does not configure instruction files, semantic
search, embedding models, semantic indexes, or language servers.

OpenAI embedding setup remains a separate user-controlled operation.
